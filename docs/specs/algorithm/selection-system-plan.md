# 智能选股系统功能建设实施看板

> 规范编号：`SPEC-ALGO-ISS-001`
> 权威定义 (SSOT)：[`selection-system-specification.md`](../../guidelines/algorithm/selection-system-specification.md)
> **实施状态**：规划中 (RFC) | 第 1 批产品裁定已登记（2026-09-21，31 项）| 第 2 批数据契约与技术方案裁定已登记（2026-09-21，18 项）| 第 3 批运行调度/数据源/Web/治理/验证裁定已登记（2026-09-21，15 项）| **第 4 批审查未明确事项已登记（2026-09-21，20 项 + 4 项补充修正）** | 里程碑基线已建立（M-01～M-05，依赖门控，不做日期排程）

## 一、规范核心定位摘要

- 本看板跟踪《智能选股系统 · 功能建设规范》(`SPEC-ALGO-ISS-001`) 的工程落地进度，覆盖：选股模型类型注册中心与用户自建模型生命周期、层级漏斗引擎与规则/触发器注册表、数据装配与交易日调度、CLI/API 与 Web 工作台、结果研究、持续跟踪与模型调优。
- 规范定义、配置模型、数据契约、API/Web 契约、权限与测试方案的权威内容一律以 [SSOT](../../guidelines/algorithm/selection-system-specification.md) 为准，本看板不复述公式、参数默认值或 Schema。
- 选股模型类型（条件树/漏斗/评分排序/组合）的 Schema、编译规则与统一中间结构 `CompiledSelectionPlan` 以 [`SPEC-ALGO-ISS-MT-001`](../../guidelines/algorithm/selection-model-types-specification.md) 为准，主规范 §3 仅保留导航与摘要。
- 本看板承载实施跟踪要素：当前能力审查基线、验收标准、实施任务拆分、发布门禁与待产品确认事项。

### 本期范围与建设定位（2026-09-21 明确）

- **系统定位**：本系统是一套**独立的、新的智能选股系统**，与既有 5A 三层漏斗（顶层 `astock screen`）等能力并行，不复用其入口命名（CLI 统一挂 `funnel` 子命令族，见 T-08）。
- **本期重点**：**系统功能建设**，核心模块为 ①模型构建器 ②层级漏斗引擎 ③可视化参数配置 ④运行调度（含数据装配、交易日调度、运行锁与跨日状态恢复）。
- **数据来源**：统一取自**本地行情数据同步体系**（`SPEC-DATA-001`）建立的**本地数据接口**；选股系统是位于该边界之上的消费者，**不得直连外部端点、不得复制供应商解析逻辑**（SSOT §11.2）。
- **全量参数化**：漏斗选股为本期**系统级核心配置模型**，一切规则与选股判定（创新高周期、高开比例区间、涨幅门槛、拐点循证时间点、各级窗口长度、覆盖率阈值、止损口径选择等）**均须以参数形式在可视化配置中可编辑**，并全部纳入 `definition_hash`；禁止任何硬编码判定参数（见 T-04 扩展）。
- **后置事项**：**结果评估与模型优化在后期阶段进行**；本期只交付导出、可视化等系统能力钩子，不交付优化结论。

## 二、当前能力审查基线


| 能力 | 当前状态 | 建设结论 |
|---|---|---|
| 模型类型注册中心 | 未具备 | 新建统一注册中心，首期支持条件与漏斗模型 |
| 用户自建和命名模型 | 未具备 | 新建创建向导、草稿、版本、激活和复制流程 |
| 条件选股维度组 | 未具备 | 建设技术面、基本面、行业面、资金面、消息面等分组 |
| AND/OR/NOT逻辑树 | 未具备 | 使用安全AST，不接受任意表达式执行 |
| 阶段列表增删与排序 | 已具备 YAML 结构 | 保留并增加 Schema、版本和迁移机制 |
| 阶段启停 | 已具备 `enabled` 读取 | Web 提供显式开关并显示影响 |
| 阶段组合逻辑 | 已具备 `all/any` | 增加嵌套规则组能力 |
| 阶段作用域 | 仅 `candidate_filter/universe_gate` | 扩展数据采样、评分和输出阶段，或以 Hook 表达 |
| 规则阈值调整 | 大部分已具备 | 补齐类型、范围、默认值和说明 Schema |
| 分钟拐点调参 | 部分可配置 | 移除最近2分钟、均价3点等硬编码窗口 |
| 配置校验 | 基础校验 | 增加规则级、跨字段和调度级校验 |
| 单阶段 CLI | 已具备 | 保留并增加 `run-all/debug/tick` |
| 自动数据装配 | 未具备 | 新建本地数据网关、DataAssembler 与数据水位检查器 |
| 自动调度 | 未具备 | 新建 Scheduler Tick 与交易日历门控 |
| Web 配置界面 | 未具备 | 新建策略工作台 |
| 运行审计 | 部分具备 JSON 证据 | 增加持久化运行、阶段和信号模型 |

## 三、验收标准


### 23.1 P0 核心闭环

- [ ] 平台能够注册并发现 `condition_tree` 与 `funnel` 两种模型类型；
- [ ] 条件模型和漏斗模型均能编译为统一层级定义，并由同一个层级漏斗引擎执行；
- [ ] 用户能够从空白创建、命名、保存和重新打开自己的模型；
- [ ] 保存草稿、发布版本、激活版本和启动运行具有独立状态与权限；
- [ ] 发布版本采用模型内单调递增版本号，并保存定义哈希、编译器版本和计划哈希；
- [ ] 已发布版本不可变，被运行引用后不可物理删除；
- [ ] 激活旧版本能够回滚，且不影响已经启动的运行；
- [ ] 草稿并发编辑通过 `base_version/draft_revision` 检测冲突；
- [ ] 条件模型支持多个维度分组和嵌套 AND/OR/NOT；
- [ ] 漏斗模型支持逐层配置规则及时间/条件触发器；
- [ ] 层数、顺序、启停通过配置生效；
- [ ] 所有默认规则参数可配置且具有 Schema；
- [ ] `run-all` 能按依赖执行完整漏斗；
- [ ] DataAssembler 能以 `finalized_local` 和 `intraday_capture` 两种模式形成四阶段一致性输入快照；
- [ ] 每次运行绑定 `data_snapshot_id`、`as_of`、快照清单、数据水位和内容哈希；
- [ ] 收盘层级只在15:35之后且目标Universe覆盖率和完整性达标后执行；
- [ ] 当前未支持的数据字段会阻止发布/激活，不能以零值、Mock或未声明代理继续运行；
- [ ] 技术指标记录引擎版本与参数，盘中输入记录实际捕获内容、来源和时间；
- [ ] Scheduler Tick 能在真实交易日正确触发；
- [ ] 同一分钟重复调用不重复执行；
- [ ] 同一模型允许多次手工执行，每次生成独立 `run_id`、配置快照和运行结果；
- [ ] 相同手工请求重试保持幂等，不同时间的新请求不被错误去重；
- [ ] 09:35～09:40输出稳定 `selected_codes`；
- [ ] 数据不足不产生信号；
- [ ] 正式运行只加载已发布且已激活的不可变版本；
- [ ] 层级漏斗引擎保存每个表达式节点和层级的筛选结果；
- [ ] 定盘数据未就绪、陈旧或缺失时按配置等待或失败关闭；盘中数据仅能通过统一 `DataBridge` 捕获；
- [ ] 运行与信号完整落盘并可恢复。

### 23.2 P1 Web 闭环

- [ ] 模型中心能够按类型展示用户模型；
- [ ] 新建向导支持“类型 → 名称 → 空白/模板”；
- [ ] 条件树编辑器与漏斗编辑器按模型类型正确加载；
- [ ] 可视化编辑、校验和激活策略；
- [ ] 单规则、单阶段和整链调试；
- [ ] 今日漏斗和运行时间线使用真实后端数据；
- [ ] 候选可下钻到规则证据；
- [ ] 运行详情能够查看每个层级或步骤的输入、通过、淘汰和异常结果；
- [ ] 周期结束后能够查看最终结果、配置快照和完整审计记录；
- [ ] 运行记录列表按时间展示同一模型的所有执行实例，并可点击进入对应详情；
- [ ] 版本管理页能够查看版本列表、活动版本、结构化差异、引用数、回滚和归档状态；
- [ ] 调度中心显示真实心跳和下一执行时间；
- [ ] 权限、审计和错误状态完整；
- [ ] 最终候选能够生成个股信息、入选证据、信号标记分析和交易策略回测；
- [ ] 建仓与持股策略以结构化规则展示，明确前提、失效条件、T+1和风险约束；
- [ ] 用户能够创建、暂停、取消和查看实时/T+N跟踪计划；
- [ ] 每个观察点能够追溯到原始信号、基准价格、数据版本和计算公式；
- [ ] 系统能够仅根据历史运行及其后续跟踪结果生成简单优化建议；
- [ ] 优化建议包含样本、证据、涉及层级/规则和风险提示，且不能自动修改模型；
- [ ] 前端不存在 Mock 回退。

### 23.3 P2 研究增强

- [ ] 不同模型版本的后续表现对比；
- [ ] 历史回放与滚动样本外验证；
- [ ] 参数敏感性分析；
- [ ] 候选流失与规则贡献可视化；
- [ ] 各T+N收益、相对基准、MFE、MAE、回撤和市场分层评价；
- [ ] 选股模型与PositionPolicy分别评价；
- [ ] 高级性能漂移检测；
- [ ] CSV/JSON报告导出。

## 四、实施任务拆分

### 阶段A：配置与核心引擎

1. 建立 `SelectionModelTypeRegistry`，注册条件模型与漏斗模型；
2. 实现草稿修订、不可变版本仓库、活动版本指针、差异和回滚；
3. 定义统一 v2 Schema、层级中间结构与迁移器；
4. 实现模型编译器，将条件树和漏斗模型转换为统一层级计划；
5. 实现层级漏斗引擎与安全 AND/OR/NOT 表达式求值器；
6. 建立规则、触发器元数据和参数校验注册表；
7. 消除分钟规则硬编码窗口；
8. 增加嵌套规则组、三态逻辑和缺失数据策略；
9. 实现统一模型运行状态机。

### 阶段B：数据与调度

1. 以 `daily_kline/sync_meta` 接入 `finalized_local`，实现一致性读取和 `UniverseWatermark`；
2. 实现 `intraday_capture`，将 `DataBridge` 标准化快照、分钟点和已支持盘口字段固化到运行目录；
3. 定义并实现证券主数据表、资金流、盘口与主动买卖量的缺失契约，未完成前保持 `unsupported`；ST 名称型排除与流通市值已具备，登记为正式字段并纳入快照清单；
4. 实现 `data_snapshot_id`、快照清单、输入切片、内容哈希与指标引擎版本留痕；
5. 接入与同步引擎一致的交易日历；
6. 实现 Scheduler Tick、15:35后定盘水位和盘中捕获就绪触发；
7. 实现运行锁、幂等和Signal Latch；
8. 完成跨交易日状态传递和恢复。

### 阶段C：API与持久化

1. 模型类型、用户模型和不可变版本仓库；
2. 运行、阶段、候选和信号存储；
3. ResultAssessment、TrackingPlan、TrackingObservation、ModelEvaluation和OptimizationSuggestion存储；
4. Selection Model API 与权限；
5. SSE运行事件；
6. 审计与健康接口。

### 阶段D：Web工作台

1. 模型中心和新建模型向导；
2. 条件树编辑器与漏斗编排器；
3. 动态规则与触发器编辑抽屉；
4. 调试器；
5. 运行时间线和候选下钻；
6. 调度中心与数据诊断；
7. 结果研究、持续跟踪和模型评估工作区（**其中结果研究/跟踪/评估工作区依赖阶段E，随二期交付**）。

### 阶段E：结果研究、跟踪与调优（二期）

> 期次裁定（C-1，2026-09-21）：本阶段移出一期范围，随二期交付；其余阶段（A/B/C/D/F）为一期。

1. 实现结果个股信息快照与入选证据聚合；
2. 复用量化引擎实现信号标记、事件驱动回测和PositionPolicy；
3. 实现实时/T+N TrackingPlan、Tracker Tick和观察存储；
4. 实现历史运行范围、T+N汇总、层级流失和规则通过/未通过组对比；
5. 实现基于历史运行与后续跟踪结果的简单优化建议，不自动修改模型。

### 阶段F：验证与发布

1. 单元、集成、前端和E2E测试；
2. 一周影子运行，不发送正式信号；
3. 本地数据同步延迟、缺失、陈旧、接口故障和调度恢复演练；
4. 开启正式信号；
5. 对简单优化建议进行人工抽样复核和审计检查。

## 五、发布门禁


以下任一条件不满足时不得标记正式可用：

1. `schedule` 已被真实调度器读取，而非仅显示在界面；
2. 用户能够从空白创建至少条件模型和漏斗模型，保存草稿、发布并激活后无需编码即可运行；
3. Web 所有数字可追溯到后端运行记录；
4. 节假日不会运行早盘策略；
5. 数据不足不会生成股票代码；
6. 重启后当日状态能够恢复；
7. 重复 Tick 不会重复信号或通知；
8. 配置错误在激活前被阻止；
9. 已发布版本不可变，活动版本切换不会影响已启动运行；
10. 正式运行与调试运行严格隔离；
11. 用户配置、缓存、最终结果和日志路径符合工作区规范；
12. 回测或历史跟踪结果不得被界面描述为未来收益保证。
13. 收盘/历史运行只读取已定盘本地库，盘中运行只读取已固化的 `intraday_capture`；两者均能用 `data_snapshot_id`、输入切片和内容哈希复现结果。
14. 实时/T+N观察可以从原始信号完整追溯，重复Tracker Tick不生成重复观察。
15. 回测计入A股交易约束和配置化摩擦成本，并通过未来数据检测。
16. 样本不足时不生成强优化结论，任何建议都不能自动修改、覆盖或激活生产版本。
17. 建仓与持股策略明确标识为研究方案，系统不存在自动实盘下单路径。
18. 同一模型的多次执行分别保存，运行列表、详情、结果和审计不会互相覆盖。
19. 简单优化建议只基于已完成的历史运行及其跟踪结果，并可追溯到具体 `run_id` 和观察样本。

## 六、待产品确认事项

1. “高开1%～2%”是否设置2%硬上限，还是仅要求高开不少于1%；
2. 默认突破周期采用20日还是60日；
3. 大盘门控固定上证指数，还是允许沪深300/中证1000按策略选择；
4. 卖压衰减是否必须取得主动买卖量，还是允许分钟涨跌量代理；
5. 09:36首次满足即输出，还是统一等待09:40后一次性输出；
6. Web 中谁有权激活策略和启停自动调度；
7. 信号通知渠道采用Web站内、桌面Toast还是外部消息渠道。
8. 条件模型首期是否允许跨维度引用派生分数，还是仅允许布尔结果引用；
9. 用户自建模型是否默认仅本人可见，后续是否支持模板共享和复制；
10. 模型名称是否允许重复；建议允许显示名称重复，但 `model_id` 必须全局唯一。
11. 证券主数据表、资金流、分钟线和盘口首期哪些扩展为本地持久化表，哪些仅使用可审计的运行捕获适配器；
12. P3全市场收盘筛选的默认覆盖率是否必须100%；建议正式信号默认100%，降低阈值必须由模型显式配置并披露缺失标的；
13. 默认跟踪哪些候选、实时采样频率以及T+N观察点；建议首期跟踪全部最终候选，默认T+1/T+3/T+5/T+10/T+20；
14. T+N默认使用开盘、收盘还是VWAP作为标记价格；建议同时保存标准口径，但模型评价固定一个主口径；
15. 建仓策略使用统一风险模板，还是允许每个模型绑定独立PositionPolicy；建议允许模型绑定模板并保存版本；
16. 模型评价触发的最小样本数、最短观察周期和默认比较基准；上线前必须由研究治理规则确定；
17. 优化建议是否提供“一键带入模型构建器”；建议首期仅支持查看和标记处理状态，由用户手工修改草稿；
18. 盘中捕获快照及实际输入切片的保留期限和容量上限；建议至少覆盖运行审计、回测与调优的最长观察周期。

> **本节状态：已全部裁定（18 项，2026-09-21）**。上述 18 项依次对应 `D-01～D-18`，裁定结论见下方「### 已裁定事项 · 第 1 批产品裁定」。本节保留原始提问措辞以便追溯，**不再作为待办来源**。

### 已裁定事项 · 第 0 批（2026-09）

- 数据准入范围：本期仅技术面、市场面与低风险项（高股价、超高PE、长期阴跌、ST名称型排除）可发布；定盘链路补齐 `PE` 与流通市值。（**其中「资金流暂不建设」已被第 1 批 D-11 取代，见下**）
- 拐点数据底线：双轨，正式信号必须使用真实主动买卖量，代理口径仅限调试预演并标记 `not_eligible_for_signal`。
- 输出时点：窗口结束（09:40）一次性收敛输出，不再于窗口内即时锁存。
- 快照保留：普通运行切片保留 90 天；信号关联切片按最长跟踪观察周期加 30 天保留。

### 已裁定事项 · 第 1 批产品裁定（2026-09-21，共 31 项）

本节裁定取代 §六 待确认条目 1～18 的对应项，并新增风控、资产归属与规格冻结类裁定。**裁定一经登记即对实现具有强制力**；新增模块不得再自行选择默认值。

> **示例来源注记**：下表中标注「**示例来源**」的定值（20 日新高周期、高开 1%～2%、主板 7% / 20cm 12%、大盘上证 MA20 等）取自[选股漏斗示例 1](../../guidelines/algorithm/selection-funnel-example-1.md)。标记「示例来源」仅表示该取值**来源于示例采用的示例口径、作为默认取值参考**，它们**均为可配置参数**（见 X-03 全量参数化），**不代表系统唯一或不可变更的规范数值**。

| 编号 | 对应 §六 | 议题 | 裁定结论 |
|:--:|:--:|---|---|
| D-01 | §26-2 | 新高周期 | 可配置；快捷预设 10/20/30/60 日 + 支持自定义 N 日；默认值待里程碑基线确认（建议 20 日，**示例来源**） |
| D-02 | §26-1 | 高开幅度 | 可配置填写；默认 `gap_min=1%`、`gap_max=2%`（**示例来源**）；不设不可配置的硬上限 |
| D-03 | — | 涨幅上限 | 可配置；默认 主板 7% / 创业板（20cm）12%（**示例来源**） |
| D-04 | §26-3 | 大盘门禁 | 仅上证综指 MA20（**示例来源**；首期不提供多指数可选） |
| D-05 | §26-4 | 卖压数据 | 双轨制 + 可配置档位（`strict`/`proxy`）；代理档强制标记 `not_eligible_for_signal`（延续第 0 批） |
| D-06 | §26-5 | 输出窗口 | 窗口端点可配置；默认不加软截止，09:40 一次收敛（延续第 0 批） |
| D-07 | §26-6 | 激活/启停权限 | 管理员 + 模型作者 |
| D-08 | §26-7 | 通知渠道 | 仅 Web 站内；首期不引入桌面 Toast 与外部消息渠道 |
| D-09 | §26-8 | 跨维度分数引用 | 双轨：默认仅布尔（`VERDICT_PASS/FAIL/UNKNOWN`）；引用派生分数须显式声明分数契约版本，编译期校验循环依赖 |
| D-10 | §26-9 / §26-10 | 可见性与命名 | 默认仅本人可见；显示名可重名，`model_id` 全局唯一 |
| D-11 | §26-11 | 首期数据持久化 | 证券主数据 + 分钟线 + 资金流**正式持久化**；盘口（五档）仅运行捕获。（**取代第 0 批「资金流暂不建设」**） |
| D-12 | §26-12 | 覆盖率阈值 | 正式信号默认 100%；阈值可配置，不达标整场降级且不输出正式信号 |
| D-13 | §26-13 | 跟踪范围/频率/观察点 | 跟踪全部最终候选；实时采样 1 分钟；观察点 T+1/T+3/T+5/T+10/T+20 |
| D-14 | §26-14 | T+N 标记价 | 多口径存储（收盘/开盘/最高/VWAP）；模型评价固定收盘为主口径 |
| D-15 | §26-15 | 建仓策略绑定 | 统一风险模板库 + 模型绑定并保存模板版本 |
| D-16 | §26-16 | 评价触发阈值 | 由研究治理规则书面确定；三项均参数可配置（默认 样本≥30、周期≥20 交易日、基准=沪深300） |
| D-17 | §26-17 | 优化建议闭环 | 仅查看 + 标记处理状态（待处理/已采纳/已忽略）；不回写、不自动建版 |
| D-18 | §26-18 | 快照保留与容量 | 普通运行 90 天 / 信号关联 = 最长跟踪周期 + 30 天；超限分级淘汰（先删无信号关联，再删最旧） |
| D-19 | — | T+1 风控 | 接受当日不可卖硬约束；采用双层风控 T-Gap（跳空预判，开盘执行）/ T-Time（盘中时间窗）/ T+1（次日开盘强制处置），三层阈值入配置 |
| D-20 | — | 仓位上限 | 主板单票 15%、20cm 单票 8%；单日合计 ≤40%；最多 3 只 |
| D-21 | — | ERS 标定 | 接受 40～60 交易日前向采集期，并行代理口径回测；输出显式标记「未标定」 |
| D-22 | — | 竞价预筛 | 纳入二期；一期仅建设 09:35 后开盘反转链路 |
| D-23 | — | 止损口径 | **不统一**：仅登记三套口径适用边界，新增模块须显式声明所引用口径（技术债保留，见下风险提示） |
| E-01 | — | `funnel` 资产归属 | 以现有 `funnel` 系列（`funnel_engine.py` / `stock_funnel.py` / `funnel_cmds.py` / `config/funnel_strategy.yaml`）为唯一实现底座；`SPEC-ALGO-003` 规划路径（`predicate.py` / `pipeline.py` / `opening_reversal/`）回写映射到该底座，不新建平行目录 |
| S-01 | — | Schema 版本 | 统一 v2 + v1→v2 自动迁移器 + 加载告警；`config/funnel_strategy.yaml`（`version: 1`）自动升级 |
| S-02 | — | 版本与哈希 | 三级分离：`version`（模型内单调递增、不可复用不可回退）/ `definition_hash`（规范化定义 JSON 的 SHA256，字段排序无关，含规则/参数/依赖声明）/ `plan_hash`（编译产物层计划 × `compiler_version` × 数据快照契约）；`compiler_version` 为代码常量，编译器逻辑变更时手工递增 |
| S-03 | — | 草稿并发 | 悲观锁：进入编辑即独占，超时自动释放；须提供显式解锁与锁状态可见 |
| S-04 | — | AST 语义 | 白名单 = `AND`/`OR`/`NOT` + 叶子谓词（比较/阈值/集合归属）；禁止任意表达式求值；`NOT` 遵循 Kleene 强三值（`NOT UNKNOWN = UNKNOWN`，不回绕为 PASS） |
| S-05 | — | 嵌套深度 | 最大 3 层（含顶层）；叶子谓词总数上限 200；超限编译期报错 |
| S-06 | — | 参数元数据 | JSON Schema 2020-12 子集 + UI 扩展注解（`x-ui-widget` / `x-quick-presets` / `x-unit` / `x-default`）；前端据注解自动渲染表单；快捷预设复用 D-01 的 10/20/30/60 日 |
| S-07 | — | 状态机 | 生命周期态 `draft → validating → compiled → active → deprecated → archived` 与运行态 `running / succeeded / failed / skipped` **分离**；运行态不回写模型状态 |

#### 第 1 批偏离推荐口径项与遗留风险

以下 3 项未采纳规范建议口径，须作为显式风险继续跟踪：

| 编号 | 偏离内容 | 风险与配套要求 |
|:--:|---|---|
| D-23 | 止损口径不做收敛 | 现有三套互斥口径并存：`config.py`（-3%/-5%）、`risk_position_manager.py`（`HARD_STOP_LOSS_PCT = -6.0`）、`AGENTS.md` 铁律（T0 -3% / T1 -5% / T2 -8%）。**配套要求**：新增选股系统模块必须在规则元数据中显式声明所引用口径；registry 增加口径来源字段；该项登记为 P0 技术债并在发布门禁中增加「止损口径可追溯」检查 |
| S-03 | 采用悲观锁而非乐观锁 | 必须配套实现「超时自动释放 + 用户显式解锁 + 锁持有者可见」；否则浏览器异常关闭将残留死锁。该项列入 §五 发布门禁可用性检查 |
| D-11 | 主数据/分钟线/资金流三类持久化 | 首期工期与存储成本上升；须为分钟线与资金流补充增量同步、去重、容量淘汰与回补校验任务。**与 D-12（覆盖率 100%）强耦合**：若增量同步未就绪，100% 门槛将长期不达标而阻断全部正式信号 |

### 已裁定事项 · 第 2 批（2026-09-21，共 18 项）

本节为第 1 批裁定后的数据契约（P 类，8 项）与技术方案（T 类，10 项）裁定，**全部采纳推荐口径，无偏离项**。裁定一经登记即对实现具有强制力。

#### 数据契约（P-01～P-08）

| 编号 | 议题 | 裁定结论 |
|:--:|:--|:--|
| P-01 | 五档买卖盘解析器 | 补齐解析器；按 D-11 仅作运行捕获，不入正式持久化表 |
| P-02 | 拐点数据档位 | 首期仅实现代理档（分钟涨跌量口径）；`strict` 仅留接口占位并强制阻断正式信号 |
| P-03 | 前向归档目录 | 独立前向归档目录，按交易日分片、只追加不可改；与运行快照解耦，不受 D-18 的 90 天淘汰影响 |
| P-04 | 集合竞价采集 | 首期采集 9:15～9:25 竞价数据，仅归档、不参与信号；采集失败不影响主链路 |
| P-05 | 分钟线写入契约 | `(code, date)` 唯一键幂等写入 + 滚动窗口（建议 60 交易日，可配置）+ 缺失日可回补 |
| P-06 | 资金流写入契约 | 与分钟线同构：`(code, date)` 唯一键 + 日频增量同步 + 可回补 |
| P-07 | 证券主数据契约 | `code` 唯一键 + 完整字段（名称/上市状态/停牌/ST/流通市值/板块归属）+ 变更历史 |
| P-08 | 数据准入水印 | 统一结构（数据来源 / 取数时点 / 可用性 `ok\|degraded\|unsupported` / 降级原因 / 是否可发正式信号）；全部 `--json` 输出统一挂载 |

#### 技术方案（T-01～T-10）

| 编号 | 议题 | 裁定结论 | 后续待做 |
|:--:|:--|:--|:--|
| T-01 | AST 引擎落地方式 | **就地升级 `funnel_engine`**：保留 `RuleRegistry`/`StageResult`/`verdict` 契约，`stage.logic` 升级为可嵌套 group 节点（`AND`/`OR`/`NOT`） | `validate_stage` 增加深度（≤3 层）与叶子总数（≤200）校验；现有 24 个测试与 `funnel_strategy.yaml` 保持向后兼容 |
| T-02 | 哈希规范 | **SHA-256 + 规范化 JSON**（键排序、去空白、数值归一）；`definition_hash` 覆盖规则结构与参数，`plan_hash` 额外覆盖 `compiler_version` 与数据快照契约；`calendar_version` 不入哈希 | 落地哈希计算与比对门禁（发布门禁 9） |
| T-03 | `run_id` 生成与落盘 | **内容派生**：触发时间戳 + `plan_hash` 短哈希；运行元数据写 `output/cache` 下 runs 目录，审计轨迹按交易日写 `log/`；`StageResult` 与 `--json` 输出统一挂载 | 满足发布门禁 19 的 `run_id` 可追溯要求 |
| T-04 | 硬编码窗口参数化 | `prices[-3:]`（均价回溯）、`points[-2:]`（recent）、`points[-4:-2]`（previous）**收敛为 stage spec 可配置参数**，默认 3/2/2 与现值一致 | 参数纳入 `definition_hash` 覆盖范围，避免改参数而哈希不变 |
| T-05 | 止损口径可追溯 | 尊重 D-23 不统一数值；每处止损常量在 registry **登记口径来源**（文件/常量名/数值/依据技能），新增校验子命令扫描全部止损常量 | 未登记口径的新增模块构建失败；发布门禁增加「止损口径可追溯」自动检查 |
| T-06 | v1→v2 迁移器 | **内存迁移**：v1 加载时自动迁移为 v2 结构，原文件不自动改写；迁移函数幂等；加载输出 WARNING 级告警；另提供显式子命令供主动回写 | 对应 S-01；存量 v1 产物按同规则迁移 |
| T-07 | 悲观锁载体 | **跨平台文件锁**（`fcntl`/`msvcrt` 封装），锁文件按 `model_id` + `draft_revision` 命名 | 配套超时自动释放、显式解锁与锁持有者可见（列入发布门禁可用性检查） |
| T-08 | 编排层 CLI 入口 | **挂到现有 `funnel` 子命令**：`funnel screen --mode opening_reversal`、`funnel regime`、`funnel trigger`；避免与顶层 `screen`（5A 三层漏斗）命名冲突 | 全部追加 `--json` 并挂载 P-08 统一水印 |
| T-09 | 五档盘口接入方式 | 在**权威解析器 `tencent_fields`** 补齐解析并输出统一 `order_book` 结构，供 L1 数据源与 funnel 链路共用；按 P-01 仅运行捕获；盘口缺失或卖盘为 0 时字段置 `None` 并标 `not_eligible_for_signal`，交 Kleene 判为 `UNKNOWN` | 不新增腾讯 L1 解析分支（维持 SSOT 收敛成果） |
| T-10 | 前端 Mock 兜底 | **删除** `MarketFallbackData` / `WatchlistFallbackData` / `ReturnsFallbackData` 三张硬编码兜底表及其分支；接口不可用时展示明确不可用提示 + 数据准入水印（P-08），不伪造行情与收益数据 | 直接满足 M-04 门禁「前端不存在 Mock 回退」 |

### 已裁定事项 · 第 3 批（2026-09-21，共 15 项）

本节为第 1、2 批裁定后，基于**实测代码缺口重新推导**的运行调度（O 类，4 项）、数据源与配额（R 类，3 项）、Web/API 契约（W 类，3 项）、治理登记（G 类，3 项）与验证发布（V 类，2 项）裁定，**全部采纳推荐口径，无偏离项**。

> 编号推导说明：原第 3 批拟用编号 `N-01～N-05 / E-02～E-09 / S-08～S-16` 在四份计划与全仓库均无登记，无法逐字复现，经确认改为依据实测代码缺口重新推导，清单经用户确认后推进。

#### 运行与调度（O-01～O-04）

| 编号 | 议题 | 裁定结论 | 后续待做 |
|:--:|:--|:--|:--|
| O-01 | 调度器载体 | **内置 Tick 常驻循环**：新增 `astock funnel daemon`，内置 Tick 并接入 `TradeCalendar` 门控，真实读取 `config/funnel_strategy.yaml` 中 4 处 `schedule` 窗口（`15:35-23:59` / `09:30-09:35` / `09:30-09:31` / `09:36-09:40`；窗口端点取值 **示例来源**：选股漏斗示例 1 的示例配置，端点可配置见 D-06）；当日状态进程内维护，配合 O-04 落盘满足恢复要求 | 满足发布门禁 1「`schedule` 已被真实调度器读取」；daemon 需支持优雅退出与运行锁（O-02） |
| O-02 | 运行锁载体与粒度 | **跨平台文件锁（与 T-07 同族实现），粒度 = `model_id + trade_date`**：允许多模型并行、同模型同交易日串行；守护进程与手工 CLI 天然互斥 | 区别于 T-07（草稿编辑锁，粒度 `model_id + draft_revision`）；列入发布门禁可用性检查（超时释放/显式解锁/持有者可见） |
| O-03 | 幂等键定义 | **分层幂等键**：调度 Tick 按 `(model_id, version, stage, trade_date, minute)` 去重；手工运行按「模型+版本+参数」请求指纹在短窗内去重 | 同时满足 §23.1「同一模型允许多次手工执行、每次独立 `run_id`」与「相同请求重试幂等、不同时间新请求不被误去重」两条互斥要求 |
| O-04 | 跨交易日状态恢复 | **落盘快照 + 按交易日重建**：当日状态（Signal Latch、已触发阶段、已发信号）随 T-03 运行元数据写 `output/cache` 下 runs 目录；进程启动按当前交易日重建，跨日快照标记过期不恢复 | 满足阶段B 第 8 条与发布门禁 6「重启后当日状态能够恢复」 |

#### 数据源与配额（R-01～R-03）

| 编号 | 议题 | 裁定结论 | 后续待做 |
|:--:|:--|:--|:--|
| R-01 | 分钟线数据源与降级链 | **新增 L1 腾讯分钟源 + 本地归档优先**：注册 `tencent_minute`（L1 零依赖，复用腾讯同族接口），`FALLBACK_CHAIN` 新增 `minute` 条目；调度 Tick 实时写前向归档（P-03/P-05），读取优先归档、缺失日回补时回退该源 | 现存 `DATA_SOURCES` 无任何分钟级源、`get_fallback("minute")` 返回空；本项为 P-05 持久化契约的数据来源前提 |
| R-02 | 资金流零依赖替代 | **新增 L1 近似档 + 保留精算档**：以腾讯 qt 已含的内外盘/成交额为 L1 零依赖近似档（`flow_source=proxy`，水印 `availability=degraded`），`proxy_patch` 保留为精算档优先使用 | 现链 `["proxy_patch", "sina_scripts"]` 首源为 L3（`requires_venv=True` 且需积分），与 D-12 100% 门槛冲突；与 P-02 代理档分级、P-08 水印结构对齐 |
| R-03 | 覆盖率计算口径 | **双口径，标的数为主**：主口径 = 有值标的数 / **当日候选池标的数**（硬门禁 100%）；同时记录流通市值加权覆盖率作参考观测，不参与门禁判定 | 分母明确为候选池以避免全市场稀释；写入 M-03 门禁校验实现 |

#### Web/API 契约（W-01～W-03）

| 编号 | 议题 | 裁定结论 | 后续待做 |
|:--:|:--|:--|:--|
| W-01 | 选股系统 SSE 事件契约 | **复用平台既有 SSE 基础设施**（同 `chat.py` 实现族），新增选股运行事件流端点 `GET /api/selection-models/runs/{run_id}/events`；事件类型 `run.started` / `stage.completed` / `run.finished` / `run.failed` / `heartbeat`，每事件挂载 `run_id` 与 P-08 水印 | 对应阶段C 第 5 条；与 T-03 的 `run_id` 内容派生口径一致 |
| W-02 | 权限码与角色映射 | **10 个权限码全量登记**（`selection_model:` `view` / `create` / `edit` / `publish` / `activate` / `run` / `track` / `evaluate` / `admin` / `debug`）；**新增角色 `model_author`** 承载 D-07 的「模型作者」，绑定 `view/create/edit/run/track/evaluate`，不授予 `publish/activate/admin/debug`；`super_admin` 全量 | 与现有 `scripts/server/api/roles.py` 权限体系对齐；`:debug` 边界见 G-02 |
| W-03 | 前端降级展示 HTTP 契约 | **统一响应包裹 + 水印驱动**：接口不可用时保持 HTTP 200 并返回 `{ data: null, error_code, watermark }`（`watermark.availability ∈ {degraded, unsupported}`），前端依 `error_code` 与水印展示明确不可用提示；网络级故障仍走 HTTP 5xx | 与 T-10（移除 Mock 兜底）、P-08（水印结构）严格对齐；避免前端将业务性不可用误判为网络故障 |

#### 治理登记（G-01～G-03）

| 编号 | 议题 | 裁定结论 | 后续待做 |
|:--:|:--|:--|:--|
| G-01 | 技能清单登记范围与能力 ID | **登记为新技能 `astock-selection-model`**（能力 ID `astock_selection_model`），CLI 入口挂 `funnel` 子命令族（T-08）；同步更新 `config/skills_manifest.json` 与 `AGENTS.md` 技能清单计数 | 对应待办 G1；不改动既有 18 项技能 ID |
| G-02 | `selection_model:debug` 权限边界 | **仅 `super_admin` 持有**；`:debug` 只允许查看编译中间产物（layer plan、`plan_hash`/`definition_hash` 对比）与规则评估轨迹，不允许修改模型、发布版本或直接发信号 | 对应待办 G3；裁定后回写 `pending-backlog-plan.md` §一 G3 状态 |
| G-03 | 研究治理规则书面化落点 | **落点 `docs/guidelines/algorithm/selection-research-governance.md`（新建）**，明确 D-16 研究治理规则与「建议不得自动改版」的评审流程：变更须经评审 + 变更日志登记，建议仅作为人工决策输入 | 对应 D-16；与阶段E（二期）的模型调优流程衔接 |

#### 验证与发布（V-01～V-02）

| 编号 | 议题 | 裁定结论 | 后续待做 |
|:--:|:--|:--|:--|
| V-01 | 影子运行「不发送正式信号」开关机制 | **双层开关 + 判定准则**：模型版本级 `shadow_mode` 标志 + 全局开关；判定准则 = Signal Latch 正常记录但**不派发下游**（无 signal dispatch 记录）、运行元数据与 `--json` 输出显式标记 `shadow=true` | 对应阶段F 第 2 条；一周影子运行期不得产生任何正式信号派发 |
| V-02 | 五类故障演练范围与通过准则 | **五类演练**：数据源全链路不可用、调度器重启恢复、运行锁竞争、哈希变更门禁阻断、前端接口不可用；**通过准则** = 每类有可复现演练脚本 + 降级行为与预期一致 + 无正式信号误发 + 审计轨迹完整可追溯 | 对应阶段F 第 3 条；与发布门禁 6、9、19 联动校验 |

### 已裁定事项 · 第 4 批（审查未明确事项，2026-09-21，共 20 项）

> **背景**：对 §一～§八 与两份 SSOT 做「未明确事项」全面审查，输出 20 项（A 组阻断级 5 项 / B 组回写级 6 项 / C 组补定义级 9 项），逐项交互裁定完毕。
> **偏离提示**：本批 **A-01 与 A-02 采纳用户自定义口径，与推荐选项不同**，已在下表显式标注「**采纳自定义口径（偏离推荐）**」，后续引用不得误读为推荐口径。

#### A 组 · 阻断级（A-01～A-05）

| 编号 | 议题 | 裁定结论 | 后续待做 |
|:--:|:--|:--|:--|
| A-01 | 模型生命周期与分享语义 | **采纳自定义口径（偏离推荐）**：区分「**模型模板**」与「**模型实例**」；模型模板**不可停用**，只能「**禁止新建实例**」（避免影响正在执行的实例）；模型分享语义 = **接收者另存为自有模型**（复制语义，避免互相串改）；`model_author` 可激活**本人**模型 | 回写 SSOT 与 T-06/T-07 版本仓库语义；`:activate` 权限边界按 C-03 矩阵 |
| A-02 | 止损数值来源 | **采纳自定义口径（偏离推荐）**：**系统提供默认止损数值**；用户可**自行设置**，默认取系统值。数值口径仍不统一，落点统一登记（见 D-23 / D-19 正交拆分 C-07） | 回写 SPEC-ALGO-003 止损口径；与门禁扫描（T-05）对齐 |
| A-03 | CLI 子命令命名口径冲突 | 以 **T-08** 为准：SPEC-ALGO-003 §2.4 回写为 `funnel screen --mode` / `funnel regime` / `funnel trigger` | 回写 SPEC-ALGO-003 计划 §2.4（B/A 组联动） |
| A-04 | `selected_codes` 定性 | **观察候选清单 ≠ 正式信号**；一期代理档仅输出候选并标 `not_eligible_for_signal`，正式信号**二期启用** | 回写 SPEC-ALGO-003 指南候选/信号定性 |
| A-05 | 覆盖率分母口径 | **分层定义分母**：`universe_gate` 层 = 该层输入 Universe（P3 目标股票池）；`candidate_filter` 层 = 上层输出池 | 与 R-03 主口径（候选池标的数）联动；P-08 水印校验 |

#### B 组 · 回写级（B-01～B-06）

| 编号 | 议题 | 裁定结论 | 后续待做 |
|:--:|:--|:--|:--|
| B-01 | SPEC-ALGO-003 计划 Q1～Q12 与主计划数值冲突 | **逐条回写** D-01～D-23 结论，删除冲突的「本设计默认」数值（如 Q3 默认 5% vs D-02 默认 2%） | 回写 SPEC-ALGO-003 计划正文 |
| B-02 | SPEC-ALGO-003 §2.2/§2.3 平行目录与主线不符 | 按 **E-01** 将 §2.2/§2.3 平行目录**回写为 funnel 实际路径** | 回写 SPEC-ALGO-003 计划正文 |
| B-03 | SSOT 指南遗留 `★待确认` | 将「新高周期 20/60、涨幅板块差异化」等 `★待确认` **替换为裁定结论**并回链本计划 | 回写 `selection-model-general-specification.md` |
| B-04 | 集合竞价遗留 `P0-阻塞` / `❌缺失` 标记 | **解除阻塞标记**，改为「**首期采集归档**」（默认开启、可配置关闭，见 C-09） | 回写 SPEC-ALGO-003 计划/指南；与 D-22 一致 |
| B-05 | 生效基线未含第 2/3 批规则 | 生效基线**追加第 2/3 批规则**（P-08、O-01～O-04、R-02/R-03、W-02/W-03 等），§六 加「**已全部裁定（18 项）**」关闭标注 | 同步本文件 §六 与「当前生效规则基线」 |
| B-06 | 编号引用口径混乱（§26 等） | 编号引用**统一为「本计划 §六」**，注明 §26 为历史编号（SSOT 无 §26，最高至 §22） | 回写 `pending-backlog-plan.md` 引用 |

#### C 组 · 补定义级（C-01～C-09）

| 编号 | 议题 | 裁定结论 | 后续待做 |
|:--:|:--|:--|:--|
| C-01 | 新高周期默认值仍未冻结 | **冻结**新高周期默认 **20 日**（数值 **示例来源**：选股漏斗示例 1，仅为默认取值），删除「待里程碑基线确认」措辞，纳入 `definition_hash` | 与 T-04 全量参数化联动 |
| C-02 | D-16 研究治理规则无落点 | **新建** `docs/guidelines/algorithm/selection-research-governance.md` 承载 D-16 规则与「建议不得自动改版」评审流程 | 新建文件；与 G-03 一致 |
| C-03 | 角色权限矩阵未全量定义 | **全量角色矩阵**：`super_admin` 全量；`model_author`（view/create/edit/run/track/evaluate + 激活本人模型）；内置「投研用户」仅 view/track；「管理员」**等同 `super_admin`**（不另建 `admin` 角色） | 回写 SSOT 权限章节与 API |
| C-04 | 权限落地机制未定 | **扩展现有菜单权限**（menu + action 两段式，复用 `role_menus`），不新建权限子系统 | 与 C-03 联动；回写 `scripts/server/api/roles.py` 设计说明 |
| C-05 | ~~腾讯同族分钟接口契约~~ | **整体撤回**：与 SSOT §11.2「选股模块不得直连外部端点、不得复制供应商解析逻辑」冲突，归**数据同步体系**职责 | 长期落库需求以逻辑契约形式向 `SPEC-DATA-001` 提出 |
| C-06 | 运行锁持有时长与 Tick 周期 | 锁**运行期持有**（运行结束即释放）+ **Tick 周期 1 分钟**（与 D-13 采样、O-03 幂等键 `minute` 对齐） | 与 O-01～O-04 联动 |
| C-07 | D-23 与 D-19 口径纠缠 | **正交拆分**：D-23 管**口径集合**、D-19 管**选口径与动作**，补映射表，不引入新数值 | 回写 SSOT 止损章节 |
| C-08 | P2 研究增强 8 项归属未定 | **全部归入阶段 E**（后期评估优化阶段），并在 §四 补列缺失项（参数敏感性分析、候选流失与规则贡献可视化、CSV/JSON 导出、性能漂移检测等） | 回写 §四 与 §七 二期说明 |
| C-09 | 集合竞价采集开关与失败影响 | 定性为**默认开启、可配置关闭**；**失败不影响主链路**；回写 D-22 删除歧义「可选」措辞 | 与 B-04 联动 |

### 补充修正 · 本期定位与全量参数化（2026-09-21）

> 本节源自用户补充信息与 Round 6 确认，**对前序第 3 批裁定构成口径修正**，后续引用以本节为准。

| 编号 | 修正项 | 修正后口径 |
|:--:|:--|:--|
| X-01 | **数据源归属**（修正 R-01 / R-02 / C-05） | 选股系统是**本地数据接口的消费者**：① 分钟 K 走现有 `intraday_capture`（`DataBridge`/动态内存 Bar），选股侧**不新增外部源**，长期落库需求以逻辑契约形式向 `SPEC-DATA-001` 提出；② 主力资金按 SSOT 允许方式——**显式选用已注册代理规则 + 证据标记口径**，或等本地 `money_flow` 契约补齐；③ C-05 腾讯端点契约**整体撤回**。原 R-01/R-02 新增 L1 直连源作废 |
| X-02 | **本期范围** | 本期 = **系统功能建设**（①模型构建器 ②层级漏斗引擎 ③可视化参数配置 ④运行调度）；**结果评估与模型优化后置**（阶段 E / 后期评估优化阶段），本期只交付导出、可视化等能力钩子 |
| X-03 | **全量参数化**（扩展 T-04） | 漏斗选股为本期**系统级核心配置模型**，一切规则与选股判定（创新高周期、高开比例区间、涨幅门槛、拐点循证时间点、各级窗口长度、覆盖率阈值、止损口径选择等）**均须以参数形式在可视化配置中可编辑**并**全部纳入 `definition_hash`**；**禁止任何硬编码判定参数** |
| X-04 | **落盘方式** | 20 项审查裁定 + 本节 4 项补充修正**一并落盘**，并同步回写 `pending-backlog-plan.md`、SPEC-ALGO-003 计划与指南，新建 `selection-research-governance.md` |

### 当前生效规则基线

**第 1 批规则**：新高周期可配置（快捷 10/20/30/60 日 + 自定义，默认 20 日）；高开幅度可配置（默认 1%～2%）；涨幅上限可配置（默认主板 7% / 20cm 12%）；大盘门禁固定上证综指 MA20；通知仅 Web 站内。

数据准入不设默认替代：上市公司财报、盘口（五档）与主动买卖量仍为 `unsupported` 时，含相应必选规则的版本不得发布或激活；ST 名称型排除与流通市值已不属此列。证券主数据、分钟线与资金流自第 1 批起纳入首期持久化建设范围（见 D-11）。

**第 2 批规则**：AST 就地升级；SHA-256 规范化 JSON 哈希（`definition_hash` / `plan_hash`）；内容派生 `run_id`；硬编码窗口参数化（T-04，经 X-03 扩展为**全量参数化**）；止损口径来源登记 + 门禁扫描；v1→v2 内存迁移；跨平台文件锁；CLI 挂 `funnel` 子命令族；五档解析收敛至权威解析器；前端移除三张 Mock 兜底表；数据装配生成 `UniverseWatermark`（P-08）。

**第 3 批规则**（经 X-01 修正后生效）：内置 Tick 常驻 daemon（`funnel daemon`）读取 YAML `schedule`；运行锁粒度 `model_id+trade_date`，**运行期持有**（C-06）；分层幂等键（含 `minute`）；落盘快照按交易日重建；覆盖率主口径为**候选池标的数**（R-03，分母按 A-05 分层定义）；SSE 运行事件契约；新增 `model_author` 角色与 10 个权限码（矩阵见 C-03）；前端降级统一响应包裹；`:debug` 仅 `super_admin`；研究治理落点 `selection-research-governance.md`；影子运行双层开关与五类故障演练。
**经 X-01 修正**：**作废** R-01 新增 L1 `tencent_minute` 直连源与 R-02 新增腾讯内外盘 L1 直连近似档；分钟 K 走现有 `intraday_capture`，主力资金走已注册代理规则并标记口径。**作废** C-05 腾讯端点契约。

**第 4 批规则**：模型模板不可停用、只能禁止新建实例，分享 = 另存为自有模型（A-01，自定义口径）；系统提供默认止损数值、用户可自行设置（A-02，自定义口径）；CLI 以 `funnel screen --mode` / `funnel regime` / `funnel trigger` 为准（A-03）；`selected_codes` = 观察候选清单 ≠ 正式信号，一期标 `not_eligible_for_signal`（A-04）；覆盖率分母分层定义（A-05）；新高周期默认冻结 **20 日**（C-01）；权限复用 `role_menus` 两段式（C-04）；D-23/D-19 正交拆分（C-07）；P2 研究增强 8 项归入阶段 E（C-08）；集合竞价默认开启、可配置关闭、失败不影响主链路（C-09）。
**未决长期跟踪**：D-23（止损口径不统一）、S-03（悲观锁）、D-11（三类数据持久化）三项偏离项持续跟踪，不隐去。

## 七、里程碑基线

> 裁定来源：C-1～C-4（2026-09-21）。**不做日期排程**，里程碑以依赖就绪与门禁通过为唯一触发条件。
> 期次切分：**一期 = 阶段 A + B + C + D + F；二期 = 阶段 E**（结果研究、持续跟踪与模型调优）。

| 里程碑 | 内容 | 依赖 | 通过门禁 |
|:--:|---|---|---|
| M-01 | 一期范围与规格冻结：31 项产品裁定登记 + S-01～S-07 规格冻结 + 期次切分确认 | 无 | 本节登记完成（已达成） |
| M-02 | 阶段A 完成：配置与核心引擎可用 | M-01 | §23.1 引擎类验收项全绿（注册发现、统一层级定义、单调版本号+三类哈希、已发布不可变、回滚、草稿并发、嵌套 AND/OR/NOT、参数 Schema 齐备、`run-all` 按依赖执行）+ 发布门禁 8、9 |
| M-03 | 阶段B 完成：数据与调度可用 | M-02 | §23.1 数据与调度类验收项全绿（双模式装配、`data_snapshot_id`/水位/内容哈希、15:35 后覆盖率达标、未支持字段阻止发布/激活、指标引擎版本留痕、Scheduler Tick 交易日门控、同分钟不重复、幂等、09:35～09:40 稳定输出、数据不足不产信号）+ 发布门禁 1、4、5、6、7、11、13 |
| M-04 | 阶段C + D 完成：API/持久化与 Web 工作台可用 | M-03 | §23.1 存储类 + §23.2 全量验收项全绿（运行/阶段/候选/信号落盘与恢复、API 与权限、SSE、审计健康、**前端不存在 Mock 回退**）+ 发布门禁 2、3、10、18 |
| M-05 | 阶段F 完成：正式可用 | M-04 | 单元/集成/前端/E2E 全通过；一周影子运行（不发送正式信号）无阻断缺陷；数据延迟/缺失/陈旧/接口故障/调度恢复演练通过；开启正式信号 + **§五 发布门禁 19 条全绿** |
| 二期 | 阶段E：结果研究、持续跟踪与模型调优 | M-05 | 发布门禁 12、14、15、16、17、19 通过（跟踪可追溯、摩擦成本回测、样本不足不出强结论、建议不自动改版、建议可追溯到 `run_id`） |

**说明**：

- 发布门禁 12、14、15、16、17、19 涉及回测、跟踪与优化建议，随阶段 E 一并进入二期；M-05 阶段需先确保这 6 条对应的功能未被提前暴露为正式能力。
- D-21 的 40～60 交易日前向采集期与开发期并行推进，不构成里程碑阻塞项，但 ERS 权重在采集完成前必须在输出中标记「未标定」。
- D-11（主数据/分钟线/资金流持久化）为 M-03 的关键路径项；若增量同步未就绪，M-03 不得通过（D-12 的 100% 覆盖率门槛依赖于此）。

## 八、变更日志

| 日期 | 变更摘要 |
|:---|:---|
| 2026-09-21 | 登记第 4 批裁定 20 项（A-01～A-05 阻断级、B-01～B-06 回写级、C-01～C-09 补定义级）+ 4 项补充修正（X-01～X-04）。**A-01、A-02 采纳用户自定义口径（偏离推荐）**：区分模型模板/实例且模板不可停用、分享=另存为自有模型；系统提供默认止损数值、用户可自行设置。**X-01 作废第 3 批 R-01（L1 `tencent_minute` 直连源）、R-02（腾讯内外盘直连近似档）与 C-05（腾讯端点契约）**，改由本地数据接口消费：分钟 K 走 `intraday_capture`、主力资金走已注册代理规则并标记口径。补充明确：本期=系统功能建设（构建器/引擎/可视化配置/运行调度），结果评估与优化后置；T-04 扩展为**全量参数化**（一切规则与判定参数均须可配置并纳入 `definition_hash`）。其余裁定：CLI 以 T-08 为准、`selected_codes`=候选≠信号、覆盖率分母分层定义、新高周期冻结 20 日、权限复用 `role_menus` 两段式、集合竞价默认开启可关闭 |
| 2026-09-21 | 登记第 3 批裁定 15 项（O-01～O-04 运行调度、R-01～R-03 数据源与配额、W-01～W-03 Web/API 契约、G-01～G-03 治理登记、V-01～V-02 验证发布），全部采纳推荐口径无偏离；确定内置 Tick 常驻 daemon（`funnel daemon`）读取 YAML `schedule`、运行锁粒度 `model_id+trade_date`、分层幂等键、落盘快照按交易日重建、新增 L1 `tencent_minute` 分钟源与腾讯内外盘资金流近似档、覆盖率主口径为候选池标的数、SSE 运行事件契约、新增 `model_author` 角色与 10 个权限码、前端降级统一响应包裹、`:debug` 仅 `super_admin`、研究治理落点 `selection-research-governance.md`、影子运行双层开关与五类故障演练 |
| 2026-09-21 | 登记第 2 批裁定 18 项（P-01～P-08 数据契约、T-01～T-10 技术方案），全部采纳推荐口径无偏离；确定 AST 就地升级、SHA-256 规范化 JSON 哈希、内容派生 `run_id`、硬编码窗口参数化、止损口径来源登记+门禁扫描、v1→v2 内存迁移、跨平台文件锁、CLI 挂 `funnel` 子命令、五档解析收敛至权威解析器、前端移除三张 Mock 兜底表 |
| 2026-09-21 | 建立里程碑基线（§七）：一期 = 阶段 A+B+C+D+F，二期 = 阶段 E；M-01～M-05 依赖门控、不做日期排程；门禁采用「验收标准 + 发布门禁」双门禁；发布门禁 12/14/15/16/17/19 随阶段 E 进二期 |
| 2026-09-21 | 登记第 1 批产品裁定 31 项（D-01～D-23、E-01、S-01～S-07）；§六 待确认条目 1～18 对应项关闭；D-11 取代第 0 批「资金流暂不建设」；新增偏离项风险提示与当前生效规则基线 |
| 2026-09-20 | 由 `docs/specs/algorithm/configurable-funnel-feature-build-spec.md` 迁入看板；规范定义正文迁至 `docs/guidelines/algorithm/selection-system-specification.md` |
